# befor runing projcet

path>npm init -y	//created package.json file

//ceated folder node_modules and file nopackage-lock.json
path>npm install express	

path>npm install mongoose	//installing mongoose db

//instll nodemon
npm install -g nodemon
npm install --location=global nodemon
npm install --save-dev nodemon

//Update Node.js and npm
path>npm install -g npm

//install all
path>npm i ejs
path>npm i body-parser
path>npm i multer
path>npm i bcrypt


>npm express-generator
>npm install -g express-generator express

path>npm install express-session

//video 12
path>npm i nodemailer
path>npm i randomstring
path>npm install dotenv

//output command
path>node index.js	// (http://127.0.0.1:3000/)
or
path>nodemon index.js

